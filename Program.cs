try{﻿
    double x = 0.5;  // Пример для значения x
    double y = 1.0;  // Пример для значения y
    double d = 2.4;  // Пример для значения d


    double R = (Math.Pow(Math.Cos(y), 2)) + (2.4 * d) / Math.Exp(y) + Math.Log(Math.Pow(Math.Sin(x), 2) + 6);
    Console.WriteLine("Значение R:" +R);
}
catch{
     Console.WriteLine("Ошибка")
    }




